#!/usr/bin/env sh
set -o errexit
set -o pipefail
set -o nounset

emojis="$XDG_DATA_HOME/emojis.tsv"
touch "$emojis"

cat "$emojis" | rofi -dmenu -i -l 10 -p " " | awk '{ORS=""; print $1}' | xclip -selection "clipboard"
